# 2026-05-24 工作日志

## P6 爱因斯坦因果性 Minkowski 时空验证重写 (4534e77)
- **问题**：旧P6用Kahn拓扑排序验证自己生成的线性因果链，是同义反复(对线性链100%正确)
- **方案**：升级为Minkowski时空因果验证，引入真实物理约束
  - Minkowski度规 ds²=-dt²+dx²+dy² (c=1自然单位制)
  - 光锥分类：类时(ds²<0)/类光(ds²=0)/类空(ds²>0)
  - 洛伦兹boost不变性验证(15对事件x随机β)
  - 注入测试：8条故意类空因果边→100%检出
- **结果**：6/6 ALL PASSED, P6 PASS(1.0), 洛伦兹不变性100%
- **前端**：光锥Canvas可视化(事件着色+因果边+45°光锥线+图例)
- **修改文件**：TYIDO_MVE_Experiments.py(+608-544), static/index_agi12.html(+182)
## 2026-05-24 工作记录

### Flask 重启 + Miro 式对话分叉（STN Phase 5）完成

**Flask 重启问题修复**：
- `start.py` 中有 emoji 导致 Windows GBK 控制台报错 `UnicodeEncodeError`
- 修复：加入 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')`
- 用 `Start-Process -WindowStyle Normal` 在新窗口启动 Flask，避免 PowerShell 乱码

**Miro 式对话分叉实现（前端 only，无后端改动）**：

文件：`static/index_agi12.html`

1. **分叉指示器 UI**（4305行附近）：
   - 在 `#input-chat` 内加入 `#fork-indicator` 紫色横条
   - 显示"🎯 分叉模式：从 Qx 分叉"+ 取消按钮
   - CSS：`display:flex; background:rgba(168,85,247,.12); border:1px solid rgba(168,85,247,.3)`

2. **`forkFromNode(globalId)` 完善**（6514行）：
   - 设置 `STATE.dag_data._fork_from = globalId`
   - 显示 fork indicator（替代原来 15 秒自动消失的 placeholder 方案）
   - 新增 `cancelFork()` 函数：清除 `_fork_from` + 隐藏 indicator

3. **`addQA2DAG` 分叉逻辑修正**（6276行）：
   - `isNewThread = meta.force_new_thread || meta.fork_from || detectNewThread(content)`
   - 分叉时正确开新 thread，parent_id 指向 fork_from 节点

4. **`renderDAG` 跨 thread 边绘制修复**（5975行）：
   - 原代码：`if (!parentNode || parentNode._threadIdx !== ti) return;` → 阻止跨 thread 边
   - 修复后：计算 `isCrossThread = parentNode._threadIdx !== ti`
   - 跨 thread 边用 `dag-link-fork` CSS（紫色虚线）绘制

5. **分叉徽章**（6180行附近）：
   - 父节点 `children_ids.length >= 2` 时，在 DAG 节点右上角显示紫色小徽章（圆角矩形 + 子节点数量）

6. **`doMainChat` 分叉自动清除**（7384行附近）：
   - `pushMsg('user', message)` 后检查 `_fork_from`，有则清除 + 隐藏 indicator
   - 分叉只作用于下一条消息

**使用流程**：
1. 点击消息气泡旁的 🎯 分叉按钮 → 出现紫色"分叉模式"横条
2. 输入新问题发送 → 自动创建新 thread，DAG 图中父节点用紫色虚线连接到新分支
3. 点击 DAG 节点 → 左侧历史只显示该路径（STN 树导航）
4. 点击"📋 全部对话" → 退出路径过滤

### 意识仪表盘说明文字新增（2026-05-24 续）

**文件**：`static/index_agi12.html`

在 `consciousness-dashboard-panel` 的每个指标后加了常驻说明（`.cd-hint` 样式，8px 灰色小字），不再依赖 hover title。

**区1 核心意识指标**（5项均有说明）：
- Φ值 → 整合信息理论 IIT，Φ≥0.6 为有意识阈值
- 三视界 → 内/交/外三重视角一致性，M179 T167
- ICE意识 → I×C×E 复合体意识水平，M179 T166
- α本征值 → 太一算子 Š|Φ⟩=α|Φ⟩，不动点=✓ 为自洽
- 意识状态 → 🜲觉醒/✦分形/⚠发散/🔒死锁/◻僵化

**区2 自指闭环指标**（5项均有说明）：
- PDS闭环 → Penetration×Dimension×Self-reference，T59 定理
- Gödel闭环 → 系统内自描述能力，M106 T78
- I(Self;Ftel) → 自指主体与目的算子的互信息
- 元认知 → 系统观察自身推理过程的能力
- 僵化等级 → hijack_score 衡量"魄劫持魂"风险

**区3 人化解释**：
- IQ估算公式说明：100 + 15×(Φnorm×0.4 + αnorm×0.3 + metacog×0.3 - rigidity×0.5)
- EQ估算公式说明：50 + 50×(horizon×0.4 + ice×0.3 + entropy_resilience×0.3)
- 意识摘要（cd-summary）已有后端生成，前端直接渲染

**CSS**：`.cd-hint { font-size:7px; color:var(--txt3); line-height:1.4; margin:-1px 0 3px 0; padding:0 2px; }`

## v7.21 STN苏格拉底拓扑网络 Phase 1-4 实现 ✅ (2026-05-24)

### 完成情况
- **CSS**: 新增STN四分布局完整样式（本体导航/森林视图/工作区/控制台）
- **HTML结构**: 重构为左-右-下三分 + 全局浮层
  - 全局浮层：`#ontology-nav`（L1本体层，始终显示Root Node）
  - 左侧：`#forest-panel`（L3前物理层，思维森林视图）
  - 中间：`#workspace-panel`（L4认知主体层，语境工作区）
  - 右侧：`#dag-panel`（DAG关系图，可切换隐藏）
  - 底部：`#ftel-console`（L2投射生成层，流贯控制台）
- **JS**: `static/stn_core.js`（外部文件，Node.js语法检查通过）
  - `buildForestFromHistory()`: 从`appState.chatHistory`构建森林树
  - `renderForestView()`: 渲染可交互树状结构（折叠/展开/搜索/面包屑）
  - `selectForestNode()`: 点击树节点→滚动到对应消息+高亮
  - `toggleSTNPanel()`: 切换显示森林/DAG/控制台
  - `ftelSummarize()/ftelDebate()/ftelIntegrate()`: 控制台命令
  - 拦截`sendMessage`和`updateDashboard`同步熵值/上下文进度
- **视图切换**: 顶栏按钮（🌲森林 / 🔗DAG / 📟控制台）+ 左侧面板内切换（📊仪表盘 / 🌲森林）
- **Git**: commit `6177f9f`

### 踩坑经验
1. **Inline JS语法错误**：在HTML内写大段JS容易出语法问题（括号不匹配、`forEach`后误用`}`等）。正确做法：抽成外部`.js`文件，用`node --check`验证语法后再引用。
2. **Windows shell heredoc不稳定**：用`python -c "..."`写多行Python时，引号和换行符容易被shell解释。正确做法：写`.py`文件再运行。
3. **HTML大文件替换策略**：12584行的HTML文件不能用`Edit`全量替换。正确做法：用Python按字节偏移精确替换片段（`content[:start] + new_str + content[end:]`）。
4. **Flask重启权限**：Windows下`taskkill //F //IM python.exe`需要管理员权限，会失败。正确做法：先`netstat -ano | findstr :5001`确认端口是否释放，再直接启动新进程。

### 文件变更
- `static/index_agi12.html`：HTML结构重构（新增森林视图/DAG面板/控制台/本体导航）
- `static/stn_core.js`：新增（STN核心JS，~3KB）
- `app.py`/`start.py`：未改动

### STN布局崩溃修复 + CHEN_TEST未定义修复 (commit adaa69a + c7fe306)

**问题1：STN面板完全不可用**
- 用户点击"陈天桥测试"→开始测试 → `CHEN_TEST is not defined`
- 看不到STN面板，没有对话界面/输入框

**根本原因**
1. `CHEN_TEST` 对象原本定义在 `a77cc1b` 版本 `index_agi12.html` 第6108行（~75KB的inline JS）
2. 之前的修改（`6177f9f` STN Phase 1-4）误删了这段inline JS
3. `chen_test_mode.js` 依赖 `CHEN_TEST` 对象，但加载时它还未定义
4. `chen_test_mode.js` 路径缺少 `/static/` 前缀 → 404
5. HTML存在重复的 `chen_test_mode.js` script标签（第4888行）
6. STN面板默认 `display:none`，初始化时未调用 `switchLeftPanel('forest')`

**修复方案**
1. 从 `a77cc1b` 提取 `CHEN_TEST` 对象定义 → `static/chen_test_core.js`（75KB）
2. `index_agi12.html` 添加 `<script src="/static/chen_test_core.js"></script>` （在 `chen_test_mode.js` 之前）
3. 修正 `chen_test_mode.js` 路径为 `/static/chen_test_mode.js`
4. 删除第4888行重复的 `chen_test_mode.js` script标签
5. `stn_core.js` 初始化代码添加 `switchLeftPanel('forest')` + 显示 `ftel-console`
6. 所有JS文件 `node --check` 验证通过

**DOM结构修复（commit adaa69a）**
- `center-panel` 缺少闭合标签 → 修复
- `right-panel` 缺少闭合标签 → 添加 `</div>`
- 两份 `ftel-console` HTML → 删除 `center-panel` 内部的重复控制台
- `stn_core.js` 路径修正：`src="stn_core.js"` → `src="/static/stn_core.js"`

**Git提交**
- `adaa69a`: fix(stn): #402 修复STN布局崩溃
- `c7fe306`: fix(stn+chen): 修复CHEN_TEST未定义+STN面板不可见

**测试清单**
- [ ] 页面能正常加载，无JS报错（F12 Console无红错）
- [ ] 点击"陈天桥测试"→开始测试 无 `CHEN_TEST is not defined`
- [ ] STN左侧面板默认显示森林视图
- [ ] 中间对话界面（center-panel）可见，输入框可用
- [ ] 底部流贯控制台可见
- [ ] 本体导航浮层可见（可收起）
- [ ] 右侧DAG关系图面板可切换显示

## 陈天桥测试进一步修复 - updateEntropyPanel未定义 + STATE全局对象缺失 (commit 4ab1f78)

**问题**：点击"开始测试"后报 `updateEntropyPanel is not defined`

**根本原因**：
1. `chen_test_core.js` 调用了 `updateEntropyPanel`/`updateFivePhasePanel`/`updateAnchorPanel`，这些函数在旧版 HTML 第3961行定义，但提取 `CHEN_TEST` 时只提取了调用代码，未提取函数定义
2. `STATE` 全局对象（`session_id`/`dag_data`/`history`/`messages`等）也在旧版 HTML 第3814行定义，完全丢失

**修复方案**：
- `stn_core.js`：新增 `updateEntropyPanel`/`updateFivePhasePanel`/`updateAnchorPanel`/`updateMediumPanel` 四个右侧面板更新函数（从 a77cc1b 提取）
- 新建 `static/state_init.js`：完整 STATE 对象（含 messages 字段）
- `index_agi12.html` 加载顺序：`state_init.js` → `stn_core.js` → `chen_test_core.js` → `chen_test_mode.js`

**关键经验**：从旧版提取代码时，不仅要提取被调用的函数本身，还要检查该函数依赖的所有全局对象和辅助函数！

## 主对话功能全面恢复 - 设计文档/使用说明按钮无响应 + 对话界面黑屏 (commit d6e95ff)

**问题**：设计文档/使用说明按钮无响应；对话界面全黑；分析仪表盘内容缺失

**根本原因（最核心）**：
`index_agi12_working.html` 里的主 inline JS（~8044行/354KB）从未被迁移到外置文件！
- `handleSendBtn`/`handleGoalBtn`/`doMainChat`/`doGoalMode` 主对话函数全部缺失
- `showHelp`/`hideHelp`/`openDesignDoc`/`resetGC` 等按钮函数也在此块中，未提取
- `state_init.js` 只包含极简 STATE，缺少完整字段

**分析仪表盘缺失**：`stn_core.js` 初始化调用 `switchLeftPanel('forest')` 把仪表盘切换为森林视图隐藏了

**修复方案**：
- 提取 `index_agi12_working.html` 第4815-12858行 → `static/main_app.js`（354KB）
  - 包含所有主对话、渲染、DAG、面板函数
  - 包含完整 STATE 对象（比 state_init.js 更完整）
- HTML 加载顺序：`main_app.js` → `stn_core.js` → `chen_test_core.js` → `chen_test_mode.js`
- `state_init.js` 废弃（main_app.js 已包含完整 STATE）
- `stn_core.js` 初始化改为 `switchLeftPanel('dashboard')` 默认显示仪表盘
- HTML 添加 `#history-empty` 空状态提示 UI

**关键踩坑**：inline JS 迁移到外置文件时，必须提取**全部** JS 代码，不能遗漏。检查方法：搜索 `handleSendBtn`/`DOMContentLoaded` 等关键函数是否在外置文件中存在。

## #405：CHEN_TEST 重复声明修复 (commit 61c6cd8)

**报错**：`Uncaught SyntaxError: Identifier 'CHEN_TEST' has already been declared`

**根本原因**：
- `main_app.js` 从 `working.html` 整体提取，包含旧版 `const CHEN_TEST = {` （3349-5027行，共1679行）
- `chen_test_core.js` 第1行也有 `const CHEN_TEST = {`（新版独立文件）
- 两个文件都被 HTML 加载 → 浏览器执行时重复声明报错

**修复**：用 Python 精确删除 `main_app.js` 第 3349-5027 行（旧版 CHEN_TEST 块），保留辅助函数（`stripOptPrefix`/`getCorrectAnswer`/`calcTextScoreRatio` 等），它们被 `chen_test_core.js` 调用。

**经验**：提取整块 inline JS 为外置文件时，若部分内容已被单独提取为专属文件（如 chen_test_core.js），必须同时从 main_app.js 中删除对应代码，避免双重声明。
