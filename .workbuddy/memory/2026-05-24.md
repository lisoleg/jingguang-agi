# 2026-05-24 工作日志

## 任务：修复 index_agi12.html 仪表盘面板不可见 + 对话区域黑屏问题

### 问题现象（用户报告）
1. 仪表盘只显示2个面板（熵+五行），其他面板不可见
2. 对话区域（#history）黑屏
3. "设计文档" 和 "使用说明" 按钮点击没反应

### 诊断过程
1. 检查 `CHEN_TEST` 重复声明问题 → 之前已删除重复定义（commit 61c6cd8）
2. 检查 `switchLeftPanel()` 函数 → 在 `stn_core.js:32` 中定义，HTML中也有旧定义（bak_402中）
3. 检查 `dashboard-content` 的 CSS → 没有 `display:none`，但需要 `display:flex` 确保可见
4. 检查 `openDesignDoc()` 函数 → `window.open` 参数格式错误（缺少窗口名称参数），且可能被浏览器拦截

### 修复内容

#### 修复1：恢复 index_agi12.html 到工作状态
- 用 `index_agi12.html.bak_402` 恢复 `index_agi12.html`
- 保留了 `CHEN_TEST` 重复声明删除的修复
- Commit: c08970d

#### 修复2：添加 dashboard-content CSS 规则
- 在 `index_agi12.html` 的 `<style>` 中添加：
  ```css
  #dashboard-content{
    display:flex;flex-direction:column;flex:1;overflow-y:auto;
  }
  ```
- 确保仪表盘内容默认可见
- Commit: a2ed006

#### 修复3：修复 openDesignDoc() 弹窗拦截问题
- 原代码：`window.open(docUrl, '_blank', 'width=1200,height=900')` → 参数格式错误
- 修复后：先尝试 `window.open(docUrl, '_blank')`，如果被拦截则降级到动态创建 `<a>` 标签点击
- 文件：`static/main_app.js` 第3307-3323行
- Commit: a2ed006

### 遗留问题
1. `switchLeftPanel()` 函数在 `index_agi12.html` (行4947) 和 `stn_core.js` (行32) 中重复定义
   - JS允许重定义（后定义的覆盖先定义的），目前没有报错
   - 但为了代码整洁，后续应该删除HTML中的旧定义
2. `dashboard-content` 的结束 `</div>` 位置没有验证 → 可能提前关闭导致面板不在内部
3. 对话区域"黑屏"可能是暗色主题的正常表现（--bg=#09090f 是深灰色）

### 下一步
让用户清除浏览器缓存后测试，如果问题仍存在：
1. 检查 `dashboard-content` 的结束标签位置是否正确
2. 删除HTML中的旧 `switchLeftPanel` 定义
3. 检查浏览器控制台是否有JS错误

### 文件变更
- `static/index_agi12.html` - 恢复 + 添加CSS规则
- `static/main_app.js` - 修复 `openDesignDoc()` 函数
- 删除临时调试文件（check_dashboard*.py）
