# 2026-05-24 工作日志

## 任务：修复 index_agi12.html 仪表盘面板不可见 + 对话区域黑屏问题

### 根因分析（#406 最终诊断）
**之前将内联JS提取到外部文件（main_app.js/stn_core.js）时，提取不完整！**
- working版本：12882行/620KB，内联8069行JS（203个函数），完全自包含
- broken版本：5371行/290KB，仅456行内联JS（只有STN相关），丢失了 ~7600行关键代码
- 丢失的关键代码包括：`STATE` 全局对象、`handleSendBtn()`、`updateAllPanels()`、
  `updateEntropyPanel()`、`updateFivePhasePanel()` 等 203 个函数
- 虽然部分函数被提取到 `main_app.js`，但**不完整且不兼容**（working版本不依赖main_app.js）

### 修复
- 用 `index_agi12_working.html`（完整自包含版本）恢复 `index_agi12.html`
- working版本所有JS内联，不依赖 main_app.js / stn_core.js
- 同时修复 `openDesignDoc()` 弹窗拦截降级方案（`<a>` 标签点击）
- Commit: 1ad9d1e

### 经验教训
1. **大文件重构（提取内联JS到外部文件）后必须完整回归测试**
2. `index_agi12_working.html` 是最可靠的完整版本，未来修改应以此为基础
3. 不要用 `index_agi12.html.bak_402` 恢复——它也是不完整的提取版本

## 任务：修复对话计数为0 + 消息发送卡住问题（#404 后续）

### 问题
- `qa-badge` 显示 "0 对话"，但对话区有用户消息
- 发送消息后按钮卡在"分析中..."，迟迟得不到答复

### 根因
1. `pushMsg` 中 `addQA2DAG` 可能抛出异常，导致 `updateQABadge()` 不执行
2. `doMainChat` 中 `pushMsg` 在 `try` 块外，异常会中断整个函数，`STATE.loading` 永久为 true
3. `updateQABadge` 没有空值保护，`threads`/`qa_pairs` 未定义时会崩溃
4. `addQA2DAG` 中直接访问 `stn.active_node_id` 没有检查 `stn` 是否存在

### 修复
- `pushMsg`: 添加 try-catch 包裹 DAG 更新逻辑，确保 `renderHistory`/`updateQABadge` 始终执行
- `addQA2DAG`: 完整 try-catch + `stn` 空值检查 + `type==='user'` 处理 + `node_type` 动态设置
- `doMainChat`: `pushMsg` 移入 try-catch，防止 `STATE.loading` 永久卡住
- `updateQABadge`: 添加空值保护链 `(STATE.dag_data && STATE.dag_data.threads) || []`
- Commit: f2381c2

## 任务：修复 DeepSeek API 500 错误 + 增强 LLM fallback（#404 后续）

### 问题
- 用户提问后收到 `[DeepSeek对话错误: 500 {"error":{"message":"Internal Server Error"...}}]`
- 前端显示原始错误信息，对用户不友好

### 根因分析
1. **DeepSeek API 间歇性 500** —— 服务端问题，非我方代码问题
2. **`chat_with_image` 无 fallback** —— `taiyi_llm_enhancer._call_llm_core` 调用 `active_backend.chat_with_image()`，失败时不会自动切换其他后端
3. **前端错误提示不友好** —— 直接显示 `[DeepSeek对话错误: 500 ...]`

### 修复
- `local_llm.py`: 为 `LocalLLM` 添加 `chat_with_image` 方法（带多后端 fallback）
  - 优先尝试活跃后端的 `chat_with_image`
  - 失败时依次尝试其他后端的 `chat_with_image` → `chat` → `generate`
  - 自动切换活跃后端到成功的后端
- `index_agi12.html`: 友好化错误提示
  - DeepSeek 500 → "服务端暂时不可用，已尝试切换备用后端"
  - 超时 → "请求超时，检查网络"
  - 未配置 API Key → "请添加 DEEPSEEK_API_KEY"
  - 所有后端不可用 → 完整排查建议
- Commit: da7a58f

### 验证
- 直接调用 `llm.chat_with_image('你好')` → DeepSeek 正常返回
- 通过 `enhancer.generate('你认为人应该活到多少岁')` → 正常返回（含太乙三答格式）
- DeepSeek 当前可用，500 为间歇性问题；fallback 机制确保下次出现问题时自动切换

## 任务：修复三个前端问题 — 响应时间/字体/分叉链（#405-407）

### 问题
1. 平均响应时间显示为纯毫秒（如15000ms），不直观
2. 分析仪表盘各面板字体太小（7-11px）
3. 执行分叉后对话关系链/DAG上没有体现分叉关系

### 修复
1. **响应时间智能单位**（`updateAgentBehaviorPanel`）：>=1000ms自动转秒显示（如 `15.3s`），<1000ms保持ms
2. **字体调大**：analysis-item 11→13px, behavior-stat-label 7→9px, behavior-stat-val 11→13px,
   behavior-tool-row/trace-step/reward 8→10px, panel-title 10→12px, analysis-key 13px
3. **分叉关系链**：
   - **核心Bug**：`forkFromNode` 预创建了空线程，`addQA2DAG` 检测到 `fork_from` 后又创建一个新线程，导致多余空线程 + 分叉QA对不在预期线程
   - 修复：去掉 `forkFromNode` 中的线程创建逻辑，统一由 `addQA2DAG` 管理
   - `renderHistory`：分叉节点（parent跨线程）显示 `⑂ 分叉自 Qx` 紫色标记
   - `renderDAG`：跨线程分叉边（紫色虚线）旁边添加 `⑂ 分叉` 文字标签
- Commit: 6a23c99

## 任务：统一思维森林和对话关系链（#408）

### 问题
- 用户看不到"思维森林"(forest-view-panel)
- `buildForestFromHistory()` 从未被核心流程自动调用，森林数据永远为空
- 用户指出："思维森林和对话关系链就是一个东西"

### 修复（Commit: a5309b0）
- **移除左侧独立的 forest-view-panel 和切换按钮**（📊/🌲 按钮已删除）
- **右侧面板从"对话关系链"更名为"思维森林"**，整合森林操作功能：
  - 搜索节点（输入框）
  - 展开/折叠全部（⊞/⊟按钮）
  - 定位活跃节点（⊹按钮）
  - 森林统计（节点数、深度）移到面板底部 footer
  - footer 新增分叉关系图例
- **pushMsg() 末尾自动调用 buildForestFromHistory()**，森林数据实时同步
- **renderDAG() 末尾同步更新森林统计**
- switchLeftPanel('forest') 兼容重定向到右侧面板
- 删除旧的 renderForestView/renderForestNode 重复函数，保留兼容版本

## 任务：专家系统集成 — 216位AI专家人格注入（#409-414）

### 来源
- [agency-agents-zh](https://github.com/jnMetaCode/agency-agents-zh): 216个中文AI专家人格Markdown文件
- 23个部门：academic(6) / engineering(35) / marketing(36) / specialized(46) 等

### 新增文件
- `expert_registry.py`: ExpertRegistry 单例 + 216位专家注册表 + 搜索/部门过滤

### 修改文件
- `app.py`: 新增5个API路由（/api/experts, /api/experts/<id>, /api/experts/search, /api/experts/departments）
- `taiyi_llm_enhancer.py`: `generate()` 新增 `expert_id` 参数 → 查询专家 system_prompt → 注入 `_build_system_prompt()`
- `index_agi12.html`: header专家按钮 + 右侧抽屉模态框 + 搜索/部门筛选 + 选择专家 → 聊天时携带 expert_id

### 关键设计
- 专家模式：`expert_id` → `expert_registry.get_system_prompt()` → 替换整个 system prompt（不是追加）
- 工具定义在专家 prompt 末尾追加（`_build_tool_definitions()` 抽取为独立方法）
- 前端：选择专家后在输入框上方显示"专家模式: XX"激活条 + header按钮高亮emoji

### API测试全部通过
- GET /api/experts → 216位专家
- GET /api/experts/search?q=写作 → 搜索结果
- GET /api/experts/academic-psychologist → 详情含 system_prompt
- GET /api/experts/departments → 23个部门及计数
