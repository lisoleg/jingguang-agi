#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
更新USER_GUIDE.md第4.5节
"""
import sys

# 读取文件
with open('USER_GUIDE.md', 'r', encoding='utf-8') as f:
    content = f.read()

# 找到第4.5节的开始和结束位置
start_marker = '### 4.5 脑图系统（对话即生长）'
end_marker = '\n---'

start_idx = content.find(start_marker)
end_idx = content.find(end_marker, start_idx)

if start_idx == -1:
    print(f'❌ 未找到标记: {start_marker}')
    sys.exit(1)

if end_idx == -1:
    print(f'❌ 未找到结束标记: {end_marker}')
    sys.exit(1)

print(f'✅ 找到第4.5节: {start_idx} - {end_idx}')

# 新的第4.5节内容
new_section = '''### 4.5 脑图系统v2（对话引用关系）

**文件**: 
- **v1**: `app_mindmap.py` + `static/index.html`
- **v2**: `app_mindmap_v2.py` + `static/index_v2.html` ⭐ **(推荐)**

#### 4.5.1 核心功能

脑图系统v2实现了"**多轮对话引用关系**"的智能脑图：

| 功能 | 说明 |
|------|------|
| **问答对编号** | 每个问答对分配唯一ID（Q1, Q2, Q3...） |
| **引用关系检测** | 显式（"继续Q1"） + 隐式（关键词重叠） |
| **树状结构** | 根据引用关系构建父子节点 |
| **交互式展示** | 节点可点击展开/折叠，显示问答详情 |

#### 4.5.2 v2版本新增功能 ⭐

| 功能 | v1 | v2 |
|------|-----|-----|
| **节点含义** | 回答要点 | 问答对（Q&A） |
| **引用检测** | ❌ 未实现 | ✅ 显式+隐式检测 |
| **树状结构** | 力导向图 | 树状结构（清晰） |
| **节点交互** | 拖拽 | 展开/折叠 + 问答详情 |

#### 4.5.3 引用关系检测

**显式引用检测**（正则匹配）：

| 模式 | 示例 | 匹配结果 |
|------|------|----------|
| `继续Q1` | "继续Q1的问题" | 引用Q1 |
| `针对问题2` | "针对问题2深入讨论" | 引用Q2 |
| `关于Q3` | "关于Q3的补充" | 引用Q3 |

**隐式引用检测**（关键词重叠度）：

算法：Jaccard相似度 = |A ∩ B| / |A ∪ B|

时间衰减因子：越近的问题权重越高

#### 4.5.4 v2界面功能

```
┌─────────────────────────────────────────────────────────────────┐
│  🌌 对话引用关系脑图系统 v2                [🔄 刷新]          │
├─────────────────────────────────────┬───────────────────────────┤
│                                     │  💬 对话界面             │
│         树状脑图区域               │  ┌─────────────────┐    │
│                                     │  │ 🤖 AI助手        │    │
│    ▼ Q1: 什么是复合体理学？       │  │ 你好！我是...   │    │
│      ▼ Q3: 继续Q1...             │  └─────────────────┘    │
│    ▼ Q2: IGCTR框架是什么？        │  ┌─────────────────┐    │
│                                     │  │ 👤 你            │    │
│                                     │  │ 什么是复合体... │    │
│                                     │  └─────────────────┘    │
├─────────────────────────────────────┴───────────────────────────┤
│  📝 问答详情                                        [发送] │
│  Q: 什么是复合体理学？                                           │
│  A: 复合体理学是...                                              │
└─────────────────────────────────────────────────────────────────┘
```

#### 4.5.5 核心功能

| 功能 | 操作方式 | 说明 |
|------|---------|------|
| **发送问题** | 输入问题 → 点击发送 | 生成Q&A节点 |
| **引用检测** | 输入"继续Q1..." | 自动检测引用关系 |
| **展开节点** | 点击节点前的▼ | 展开子节点 |
| **折叠节点** | 点击节点前的▶ | 折叠子节点 |
| **查看详情** | 点击节点 | 下方显示完整Q&A |

#### 4.5.6 使用示例

**示例1：无引用（独立问题）**
```
用户输入："什么是复合体理学？"（Q1）
→ 树结构：root → Q1

用户输入："IGCTR框架是什么？"（Q2）
→ 树结构：root → Q1, Q2
```

**示例2：显式引用**
```
用户输入："继续Q1，详细解释刘原理"（Q3）
→ 检测到显式引用Q1
→ 树结构：root → Q1 → Q3, Q2
```

**示例3：隐式引用**
```
用户输入："Q1和Q2有什么关系？"（Q4）
→ 检测到隐式引用Q1和Q2
→ 树结构：root → Q1 → Q4, Q2 → Q4
```

#### 4.5.7 API端点

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/chat_v2` | POST | v2对话接口，返回AI回答+对话树 |
| `/api/mindmap` | GET/POST | 获取当前对话的树状结构 |
| `/api/node_chat` | POST | 节点追问接口（针对某个节点深入提问） |
| `/api/state` | GET | 返回系统状态 |

**示例请求**:
```bash
# 发送对话
curl -X POST http://localhost:5002/api/chat_v2 \
  -H "Content-Type: application/json" \
  -d "{\"message\": \"什么是复合体理学？\", \"session_id\": \"default\"}"

# 获取对话树
curl http://localhost:5002/api/mindmap?session_id=default
```

#### 4.5.8 技术亮点

1. **显式+隐式引用检测**：双重检测，提高准确率
2. **Jaccard相似度**：简单高效的关键词重叠计算
3. **时间衰减因子**：越近的问题权重越高
4. **树状结构**：清晰展示对话引用关系
5. **实时更新**：每次对话后自动重构树

---

'''

# 替换
new_content = content[:start_idx] + new_section + content[end_idx:]

# 写入文件
with open('USER_GUIDE.md', 'w', encoding='utf-8') as f:
    f.write(new_content)

print(f'✅ USER_GUIDE.md 第4.5节已更新')
print(f'   替换范围: {start_idx} - {end_idx}')
print(f'   新内容长度: {len(new_section)}')
